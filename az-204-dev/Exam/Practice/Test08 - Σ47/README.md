# Test08 - Σ47

---

## Questions
|n|Question|Answer|
|-|--------|-------|
|1|You develop an Azure solution that uses Cosmos DB.<br/>The Cosmos DB container must be replicated and must use a partition key that is optimized for queries.<br/>You need to implement a change feed processor solution to handle each batch of changes.<br/>Which change feed processor should you use?<br/><br/>a. Host<br/>b. Delegate<br/>c. Lease container<br/>d. Monitored container|<details><summary>Answer</summary>b. Delegate<br/><br/>There are four main components of implementing the change feed processor:<br/>1. **The monitored container:**he monitored container has the data from which the change feed is generated. Any inserts and updates to the monitored container are reflected in the change feed of the container.<br/>2. **The lease container**:he lease container acts as a state storage and coordinates processing the change feed across multiple workers. The lease container can be stored in the same account as the monitored container or in a separate account.<br/>3. **the host**:A host is an application instance that uses the change feed processor to listen for changes. Multiple instances with the same lease configuration can run in parallel, but each instance should have a different **instance name**<br/>4. **The delegate**:The delegate is the code that defines what you, the developer, want to do with each batch of changes that the change feed processor reads.<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/sql/change-feed-processor</details>|
|2|You are developing an application that uses Azure Key Vault to store keys, secrets, and certificates.<br/>The application uses the Azure Key Vault APIs.<br/>You need to protect the accidental deletion of the key vault or key vault objects. The objects in the Key vault must be retained for 90 days after deletion.<br/>You need to enable retention period and accidental deletion.<br/>Which Azure Key Vault feature should you use?<br/><br/>a. Access Policy<br/>b. Purge Protection<br/>c. Soft Delete<br/>d. SAS|<details><summary>Answer</summary>c. Soft Delete<br/><br/>Key Vault's soft-delete feature allows recovery of the deleted vaults and deleted key vault objects (for example, keys, secrets, certificates), known as soft-delete.<br/>Specifically, we address the following scenarios: This safeguard offer the below protection.<br/>Once a secret, key, certificate, or key vault is deleted, it will remain recoverable for a configurable period of 7 to 90 calendar days. If no configuration is specified the default recovery period will be set to 90 days. This provides users with sufficient time to notice an accidental secret deletion and respond.<br/>https://docs.microsoft.com/en-us/azure/key-vault/general/soft-delete-overview</details>|
|3|You are developing an application that uses Azure Key Vault to store keys, secrets, and certificates.<br/>The application uses the Azure Key Vault APIs.<br/>You need to protect the accidental deletion of the key vault or key vault objects. The objects in the Key vault must be retained for 90 days after deletion.<br/>You need to enforce retention period and accidental deletion.<br/>Which Azure Key Vault feature should you use?<br/><br/>a. Access Policy<br/>b. Purge Protection<br/>c. Soft Delete<br/>d. SAS|<details><summary>Answer</summary>b. Purge Protection<br/><br/>When purge protection is on, a vault or an object in the deleted state cannot be purged until the retention period has passed. Soft-deleted vaults and objects can still be recovered, ensuring that the retention policy will be followed.<br/>https://docs.microsoft.com/en-us/azure/key-vault/general/soft-delete-overview#purge-protection</details>|
|4|You develop an application that uses a user delegation SAS token to communicate with Azure Blob storage.<br/>You suspect that the token is compromised.<br/>You need to revoke the token.<br/>What are two possible ways to achieve this goal?<br/><br/>a. Revoke the delegation key<br/>b. Delete the stored access policy<br/>c. Regenerate the account key<br/>d. Remove the role assignment for the security principal|<details><summary>Answer</summary>a. Revoke the delegation key<br/>d. Remove the role assignment for the security principale<br/><br/>If you believe that a SAS has been compromised, then you should revoke the SAS. You can revoke a user delegation SAS either by revoking the user delegation key, or by changing or removing RBAC role assignments for the security principal used to create the SAS.<br/>https://docs.microsoft.com/en-us/rest/api/storageservices/create-user-delegation-sas#revoke-a-user-delegation-sas</details>|
|5|You plan to develop a solution that uses a multi-partitioned Azure Cosmos DB database.<br/>You plan to use the latest Azure Cosmos DB SDK for development.<br/><br/>The solution must satisfy the below requirements:<br/>Send insert and update operations to an Azure Blob storage account.<br/>Immediately process changes to all partitions.<br/>Allow parallelization of change processing.<br/><br/>You need to process the Azure Cosmos DB operations. What are two possible ways to achieve this goal?<br/><br/>a. Create an Azure App Service API & implement the change feed estimator of the SDK.Scale the API by using multiple Azure App Service instances<br/>b. Create a background job in an Azure Kubernetes Service & implement the change feed feature of the SDK<br/>c. Create an Azure Function to use a trigger for Azure Cosmos DB.Configure the trigger to connect to the container<br/>d. Create an Azure Function that uses a FeedIterator object that processes the change feed by using the pull model on the container.Use a FeedRange object to parallelize the processing of the change feed across multiple functions|<details><summary>Answer</summary>c. Create an Azure Function to use a trigger for Azure Cosmos DB. Configure the trigger to connect to the container<br/>d. Create an Azure Function that uses a Feedterator object that processes the change feed by using the pull model on the container. Use a FeedRange object to parallelize the processing of the change feed across multiple functions<br/><br/>You can work with the Azure Cosmos DB change feed using either a push model or a pull model. With a push model, the change feed processor pushes work to a client that has business logic for processing this work.<br/><br/>There are two ways you can read from the change feed with a push model: Azure Functions Cosmos DB triggers and the change feed processor library. Azure Functions uses the change feed processor behind the scenes, so these are both very similar ways to read the change feed.<br/><br/>The change feed pull model allows you to consume the change feed at your own pace. Changes must be requested by the client and there is no automatic polling for changes. Using the change feed pull model, you get more low level control of the change feed. When reading the change feed with the pull model, you have three options:<br/>* Rad changes for an entire container<br/>* Read changes for a specific FeedRange<br/>* Read changes for a specific partition key value<br/><br/>In the change feed pull model, you can use the FeedRange to parallelize the processing of the change feed. A FeedRange represents a range of partition key values.<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/sql/read-change-feed<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/sql/change-feed-pull-model#using-feedrange-for-parallelization</details>|
|6|You have several functions in an Azure Function app that uses the latest runtime host. The Azure Function app runs in a Consumption plan.<br/>The functions contain several REST API endpoints secured by using SSL.<br/>You need to monitor the availability and responsiveness of the functions.<br/>An email alert must be sent when any of the function endpoints are unavailable or responding too slowly.<br/>What should you do?<br/><br/>a. Create a URL ping test<br/>b. Create a timer triggered function that calls TrackAvailability() & send the results to Application Insights<br/>c. Create a timer triggered function that calls Getmetric("Request Size") & send the results to Application Insights<br/>d. Add a new diagnostic setting to the Azure Function app.Enable the FunctionAppLogs & Send to Log Analytics options|<details><summary>Answer</summary>a. Create a URL ping test<br/><br/>The URL ping test uses advanced HTTP request functionality to validate whether an endpoint is responding. They measure the performance associated with that response. They also add the ability to set custom success criteria, coupled with more advanced features like parsing dependent requests and allowing for retries.<br/>https://docs.microsoft.com/en-us/azure/azure-monitor/app/monitor-web-app-availability#actions-if-you-see-failures</details>|
|7|You have the following PowerShell script:<br/>`$source = New-AzScheduledQueryRuleSource -Query 'Heartbeat \| where TimeGenerated > ago(1h)' -DataSourceId "yoursource"`<br/>`$schedule = New-AzScheduledQueryRuleSchedule -FrequencyInMinutes 60 -TimeWindowInMinutes 60`<br/>`$triggerCondition = New-AzScheduledQueryRuleTriggerCondition -ThresholdOperator "LessThan" -Threshold 5`<br/>`$aznsActionGroup = New-AzScheduledQueryRuleAznsActionGroup -ActionGroup "youractiongroup" -EmailSubject "Alert EMail Subject" -CustomWebhookPayload "{'"alert'":'"#alertrulename'", '"IncludeSearchResults'":true}"`<br/>`$alertingAction = New-AzScheduledQueryRuleAlertingAction -AznsAction $aznsActionGroup -Severity "3" -Trigger $triggerCondition`<br/>`New-AzScheduledQueryRule -Location "West Europe" -Action $alertingAction -Enabled $true -Description "Alert Description" -Schedule $schedule -Source $source -Name "Alert Name"`<br/><br/>Select Yes if the below statement is true. Otherwise, select No.<br/>1. A log alert is created that sends an email when the CPU percentage is above 60 percent for five minutes<br/>2. A log alert is created that sends an email when the number of virtual machine heartbeats in the past hour is less than five<br/>3. The log alert is scheduled to run every two hours<br/><br/>a. yes, no, yes<br/>b. no, yes, yes<br/>c. no, yes, no<br/>d. yes, yes, no<br/>e. yes, no, no|<details><summary>Answer</summary>c. no, yes, no<br/><br/>1. The log alert is created based on heartbeat. Not based on CPU percentage<br/>$source = New-AzScheduledQueryRuleSource -Query 'Heartbeat \| where imeGenerated > ago(1h)' -DataSourceId "yoursource"<br/>2. The log alert is created based on heartbeat. Threshold is less than 5 and frequency is 60 minutes<br/>3. The schedule is 60 minutes<br/>https://docs.microsoft.com/en-us/powershell/module/az.monitor/new-azscheduledqueryrule?view=azps-6.4.0</details>|
|8|You are developing an online ordering application that uses Azure Cosmos DB to store orders.<br/>The latest SDK is used to manage the orders in the database.<br/>You create a new Azure Cosmos DB instance.<br/>You include a valid endpoint and valid authorization key to an appSettings.json file in the code project.<br/>You are evaluating the below application code<br/>`L01 using System;`<br/>`L02 using System.Threading.Tasks;`<br/>`L03 using Microsoft.Azure.Cosmos;`<br/>`L06 using Microsoft.Extensions.Configuration;`<br/>`L07 using Newtonsoft.Json;`<br/>`L08 `<br/>`L09 namespace salesOrders`<br/>`{`<br/>`L10 	public class salesOrders`<br/>`	{`<br/>`		...`<br/>`	}`<br/>`L11 	internal class ManageSalesOrders`<br/>`	{`<br/>`L12 		private static async Task GenerateSalesOrders()`<br/>`L13 		{`<br/>`L11 IConfigurationRoot Configuration = new ConfigurationBuilder().AddJsonFile("appSettings.json").Build();`<br/>`			string endpoint = Configuration("EndPointUrl");`<br/>`			string authKey = Configuration("AuthorizationKey");`<br/>`			using CosmosClient client = new CosmosClient(endpoint, authKey);`<br/>`			Database database = null;`<br/>`			using (await client.GetDatabase("SalesOrders").DeleteStreamAsync()) { }`<br/>`L22 			database = await client.CreateDatabaseIfNotExistAsync("SalesOrders");`<br/>`L23 			container container1 = await database.CreateContainerAsync(id: "Container1", partitionKeyPath:"/AccountNumber");`<br/>`L24 			container container2 = await database.CreateContainerAsync(id: "Container2", partitionKeyPath:"/AccountNumber");`<br/>`L25 			salesOrders saleOrder1 = new SalesOrder() { AccountNumber = "123456" };`<br/>`L26 			await container1.CreateItemAsync(saleOrder1, new PartitionKey(salesOrder1.AccountNumber));`<br/>`L27 			SalesOrder salesOrder2 = new SalesOrder() { AccountNumber = "654321" };`<br/>`L28			await container1.CreateItemAsync(salesOrder2, new PartitionKey(salesOrder2.AccountNumber));`<br/>`L29 			SalesOrder salesOrder3 = new SalesOrder() { AccountNumber = "109876" };`<br/>`L30 			await container2.CreateItemAsync(salesOrder3, , new PartitionKey(salesOrder3.AccountNumber));`<br/>`			_= await database.CreateUserAsync("User1");`<br/>`			User user1 = database.GetUser("User1");`<br/>`			_= await user1.ReadAsync();`<br/>`		}`<br/>`	}`<br/>`}`<br/>Select Yes if the below statement is true. Otherwise, select No.<br/><br/>1. A database named SalesOrders is created and it will contain two containers.<br/>2. Container1 will contain two items.<br/>3. Container2 will contain one item.<br/><br/>a. yes, no, yes<br/>b. yes, yes, yes<br/>c. no, yes, no<br/>d. yes, yes, no<br/>e. yes, no, no|<details><summary>Answer</summary>b. yes, yes, yes<br/><br/>1. The createDatabaseIfNotExistsAsync method checks if a database exists, and if it doesn't, create it. (Line 22)<br/>The Database.CreateContainerAsync method creates a container as an asynchronous operation in the Azure Cosmos service. (Line 23 and 24)<br/>2. The CosmosContainer.CreateItemAsync method creates an item as an asynchronous operation in the Azure Cosmos service. (Line 26 and 28)<br/>3. The CosmosContainer.CreateItemAsync method creates an item as an asynchronous operation in the Azure Cosmos service. (Line 30)<br/>https://docs.microsoft.com/en-us/dotnet/api/microsoft.azure.cosmos.cosmosclient.createdatabaseifnotexistsasync?view=azure-dotnet<br/>https://docs.microsoft.com/en-us/dotnet/api/microsoft.azure.cosmos.database.createcontainerasync?view=azure-dotnet<br/>https://docs.microsoft.com/en-us/dotnet/api/azure.cosmos.cosmoscontainer.createitemasync</details>|
|9|You are developing an Azure application that access a secret stored in an Azure Key Vault.<br/>How should you complete the below code segment?<br/>`string KeyVaultName = Environment.GetEnvironmentVariable("KEY_VAULT_NAME");`<br/>`var kvUri = "https://" + KeyVaultName + ".vault.azure.net;`<br/>` `<br/>`var client = new <CodeBlock1> (new Uri(kvUri), new <CodeBlock2>);`<br/><br/>a. CodeBlock1:SecretClient<br/>b. CodeBlock1:CloudClient<br/>c. CodeBlock1:ClientSecretCredential<br/>d. CodeBlock2:SecretClient<br/>e. CodeBlock2:CloudClient<br/>f. CodeBlock2:DefaultAzureCredential|<details><summary>Answer</summary>a. CodeBlock1:SecretClient<br/>f. CodeBlock2:DefaultAzureCredential<br/><br/>https://docs.microsoft.com/en-us/azure/key-vault/secrets/quick-create-net</details>|
|10|You have a premium block blob Azure storage account.<br/>You apply the following lifecycle manage policy.<br/>You need determine the implications of applying the below rules to the data.<br/>`{`<br/>`	"rules":`<br/>`	{`<br/>`		"name": "agingDataRule",`<br/>`		"enabled": true,`<br/>`		"type": "Lifecycle",`<br/>`		"definition":`<br/>`			"filters": {`<br/>`				"blobTypes": [ "blockblob" ],`<br/>`				"prefixMatch": [ "container1/salesorders", "container2/inventory" ]`<br/>`			},`<br/>`			"actions": {`<br/>`				"baseBlob": {`<br/>`					"tierToCool": { "dayAfterModificationGreaterThan": 60 },`<br/>`					"tierToArchive": { "dayAfterModificationGreaterThan": 120 }`<br/>`				}`<br/>`			}`<br/>`	}`<br/>`},`<br/>`{`<br/>`	"enabled": true`<br/>`	"name": "lastAccessedDataRule",`<br/>`	"type": "Lifecycle",`<br/>`	"definition": {`<br/>`		"actions": {`<br/>`			"baseBlob": {`<br/>`				"enableAutoTierToHotFromCool": true,`<br/>`				"tierToCool": {`<br/>`					"dayAfterLastAccessTimeGreaterThan": 30`<br/>`				}`<br/>`			}`<br/>`		},`<br/>`		"filters": {`<br/>`			"blobTypes": [ "blockBlob" ]`<br/>`		}`<br/>`	}`<br/>`},`<br/>`{`<br/>`	"rules": [`<br/>`		{`<br/>`			"name": "expirationDataRule",`<br/>`			"enabled": true,`<br/>`			"type": "Lifecycle",`<br/>`			"definition": {`<br/>`				"filters": {`<br/>`					"blobTypes": [ "blockBlob" ]`<br/>`				},`<br/>`				"actions": {`<br/>`					"baseBlob": {`<br/>`						"delete": { "dayAfterMoficationGreaterThan": 730 }`<br/>`					}`<br/>`				}`<br/>`			}`<br/>`		}`<br/>`	]`<br/>`}`<br/>Select Yes if the below statement is true. Otherwise, select No.<br/>1. Block blobs prefixed with container1/salesorders or container2/inventory which have not been modified in over 60 days are moved to cool storage.  Blobs that have not been modified in 120 days are moved to the archive tier.<br/>2. Blobs are moved to cool storage if they have not been accessed for 30 days<br/>3. Blobs will automatically be tiered from cool back to hot if accessed again after being tiered to cool.<br/>4. All block blobs older than 730 days will be deleted.<br/><br/>a. no, yes, yes, no<br/>b. no, yes, yes, yes<br/>c. yes, yes, yes, yes<br/>d. yes, yes, yes, no<br/>e. yes, no, yes, no<br/>f. yes, yes, no, no|<details><summary>Answer</summary>d. yes,yes,yes,no<br/><br/>1. Refer line numbers L10, L14 and L15. The prefix match is container1/salesorders or container2/inventory and conditions are if a file is not modified in last 60 days, data will be moved to cool tier and if a file is not modified in last 102 days, data will be moved to archive tier.<br/>2. Refer line number L29. The condition to move data to cool tier is if it is not accessed in last 30 days<br/>3. The enableAutoTierToHotFromCool property is a Boolean value that indicates whether a blob should automatically be tiered from cool back to hot if it is accessed again after being tiered to cool.<br/>4. he delete condition is for blobs that are not modified in last 730 days.<br/>https://docs.microsoft.com/en-us/azure/storage/blobs/lifecycle-management-overview?tabs=azure-portal</details>|
|11|You have an Azure Web app that uses Cosmos DB as a data store. You create a CosmosDB container by running the following PowerShell script:<br/>`$resourceGroupName = "testResourceGroup"`<br/>`$accountName = "testCosmosAccount"`<br/>`$databaseName = "testDatabase"`<br/>`$containerName = "testContainer"`<br/>`$partitionKeyPath = "/EmployeeId"`<br/>`$autoscaleMaxThroughput = 5000`<br/><br/>`New-AzCosmosDBSqlContainer -`<br/>`-ResourceGroupName $resourceGroupName`<br/>`-AccountName $accountName`<br/>`-DatabaseName $databaseName`<br/>`-Name $containerName`<br/>`-PartitionKeyKind Hash`<br/>`-PartitionKeyPath $partitionKeyPath`<br/>`-AutoscaleMaxThroughput $autoscaleMaxThroughput`<br/><br/>You create the following queries that target the container:<br/>`SELECT * FROM c WHERE c.EmployeeId > '12345'`<br/>`SELECT * FROM c WHERE c.UserID = '12345'`<br/><br/>Select Yes if the below statement is true. Otherwise, select No.<br/>1. The minimum throughput for the container is 400 RU/s<br/>2. The query SELECT * FROM c WHERE c.EmployeeId > '12345' is an in-partition query<br/>3. The querySELECT * FROM c WHERE c.UserID = '12345' is a cross partition query<br/><br/>a. yes, yes, yes<br/>b. no, yes, yes<br/>c. no, no, yes<br/>d. yes, no, yes<br/>e. yes, no, no<br/>f. no, yes, no|<details><summary>Answer</summary>c. no,no,yes<br/><br/>1. The autoscaleMaxThroughput is set to 5000. The system automatically scales the throughput T such that 0.1* Tmax <= T <= Tmax. For example, if you set autoscale maximum RU/s of 5000 RU/s, the system will scale between 500 - 5000 RU/s.<br/>2. In order to be an in-partition query, the query must have an equality filter that includes the partition key<br/>3. PartitionKey is EmployeeId not user Id. So, this query will fan-out to all physical partitions where it is run against each partition's index.<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/sql/how-to-query-container#in-partition-query<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/sql/how-to-query-container#cross-partition-query</details>|
|12|You are developing a web application that uses Microsoft Graph API.<br/>The application is registered in the Azure portal.<br/>You upload a valid X509 certificate<br/>You create an appsettings.json file containing the certificate name, client identifier for the application, and the tenant identifier of the Azure Active Directory (Azure AD).<br/>You create a method named ReadCertificate to return the X509 certificate by name.<br/>You need to implement code that acquires a token by using the certificate.<br/>How should you complete the code segment?<br/>`AuthenticationConfig config = AuthenticationConfig.ReadFromJsonFile("appsettings.json");`<br/>`X509Certificate2 certificate = ReadCertificate(config.CertificateName);`<br/>`var app = <CodeBlock1>`<br/>`  .WithCertificate(certificate)`<br/>` .WithAuthority(new Uri(config.Authority))`<br/>`.Build();`<br/>`string[] scopes = new string[] { $"{config.ApiUrl}.default" };`<br/>`AuthenticationResult result = await app.AcquireTokenForClient(<CodeBlock2>).ExecuteAsync();`<br/><br/>a. CodeBlock1:ConfidentialClientApplicationBuilder<br/>b. CodeBlock1:GetAccountAsync()<br/>c. CodeBlock1:ConfidentailClientApplication<br/>d. CodeBlock2:scopes<br/>e. CodeBlock2:app<br/>f. CodeBlock2:config|<details><summary>Answer</summary>a. CodeBlock1:ConfidentialClientApplicationBuilder<br/>d. CodeBlock2:scopes<br/><br/>https://docs.microsoft.com/en-us/azure/active-directory/develop/scenario-daemon-app-configuration?tabs=dotnet#instantiate-the-confidential-client-application-with-a-client-certificate<br/>https://docs.microsoft.com/en-us/azure/active-directory/develop/scenario-daemon-acquire-token?tabs=dotnet</details>|
|13|You are developing an application that uses Microsoft Graph SDK.<br/>You need to retrieve the user profile information from Azure Active Directory.<br/><br/>In which order should you perform the below actions?<br/><br/>1. Create a new instance of the GraphServiceClient<br/>2. Create an authentication provider by passing a client application<br/>3. Invoke the request to the Microsoft Graph API<br/>4. Register the application with the Azure Active Directory<br/>5. Build a client application by using the client app ID.<br/><br/>a. 1,3,2,5,4<br/>b. 5,1,2,4,3<br/>c. 4,5,2,1,3<br/>d. 4,5,1,2,3<br/>e. 5,4,2,1,3|<details><summary>Answer</summary>c. 4,5,2,1,3<br/><br/>To authenticate with the Microsoft identity platform endpoint, you must first register your app.<br/>Build a client application<br/>Create an authentication provider by passing in a client application and graph scopes.<br/>Create a new instance of GraphServiceClient with the authentication provider.<br/>Make a call to the Microsoft Graph API.<br/>https://docs.microsoft.com/en-us/graph/auth-v2-service<br/>https://docs.microsoft.com/en-us/graph/sdks/create-client?tabs=CS</details>|
|14|Your company has an azure subscription that includes several azure resources.<br/>Your team member has deployed an Azure virtual machine and Storage account using an Azure Resource Manager template<br/>You need to identify the Azure Resource Manager template that your team member used.<br/><br/>To achieve the requirement, you access the Container blade.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>You need to check in Deployments under Resource Group<br/>Azure Resource Manager enables you to view your deployment history. You can examine specific operations in past deployments and see which resources were deployed.<br/>https://docs.microsoft.com/en-us/azure/azure-resource-manager/templates/deployment-history?tabs=azure-portal</details>|
|15|Your company has an azure subscription that includes several azure resources<br/>Your team member has deployed an Azure virtual machine and Storage account using an Azure Resource Manager template<br/>You need to identify the Azure Resource Manager template that your team member used.<br/><br/>To achieve the requirement, you access the Virtual Machine blade.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>no<br/><br/>You need to check in Deployments under Resource Group<br/>Azure Resource Manager enables you to view your deployment history. You can examine specific operations in past deployments and see which resources were deployed.<br/>https://docs.microsoft.com/en-us/azure/azure-resource-manager/templates/deployment-history?tabs=azure-portal</details>|
|16|Your company has an azure subscription that includes several azure resources<br/>Your team member has deployed an Azure virtual machine and Storage account using an Azure Resource Manager template.<br/>You need to identify the Azure Resource Manager template that your team member used.<br/><br/>To achieve the requirement, you access the Resource Groups blade.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>You need to check in Deployments under Resource Group<br/>Azure Resource Manager enables you to view your deployment history. You can examine specific operations in past deployments and see which resources were deployed.<br/>https://docs.microsoft.com/en-us/azure/azure-resource-manager/templates/deployment-history?tabs=azure-portal</details>|
|17|You plan to develop an Azure WebJob that will run on the same instances as a web app.The webjob type should allow for the option to restrict the WebJob to a single instance.<br/>You want to make use of a suitable WebJob type.<br/><br/>To achieve the requirement, you configure the use of the Triggered WebJob type.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>It must be of type continuous. You can optionally restrict the WebJob to a single instance in continuous type.<br/>https://docs.microsoft.com/en-us/azure/app-service/webjobs-create#webjob-types</details>|
|18|You plan to develop an Azure WebJob that will run on the same instances as a web app.<br/>The webjob type should allow for the option to restrict the WebJob to a single instance.<br/>You want to make use of a suitable WebJob type.<br/><br/>To achieve the requirement, you configure the use of the Continuous WebJob type.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>It must be of type continuous. You can optionally restrict the WebJob to a single instance in continuous type.<br/>https://docs.microsoft.com/en-us/azure/app-service/webjobs-create#webjob-types</details>|
|19|You have an Azure SQL Database named Database1.<br/>The database contains a table named Customers.<br/>The Customer table has a column named email_address.<br/>You want to implement dynamic data masking to hide the data in the email_address column.<br/><br/>To achieve the requirement, you run the below T-SQL statement:<br/>ALTER TABLE [dbo].[ Database1].[Customers]<br/>ALTER COLUMN [email_address]<br/>ADD MASKED WITH (FUNCTION = 'email()')<br/><br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>The email masking method exposes the first letter of an email address and the constant suffix ".com", in the form of an email address. aXXX@XXXX.com.<br/>https://docs.microsoft.com/en-us/sql/relational-databases/security/dynamic-data-masking?view=sql-server-ver15</details>|
|20|You have an Azure SQL Database named Database1.<br/>The database contains a table named Customers.<br/>The Customer table has a column named email_address.<br/>You want to implement dynamic data masking to hide the data in the email_address column.<br/><br/>To achieve the requirement, you run the Set-AzSqlDatabaseDataMaskingPolicy -DatabaseName " Database1" Powershell cmdlet<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>The Set-AzSqlDatabaseDataMaskingPolicy cmdlet sets the masking policy for the database.</details>|
|21|You have an Azure SQL Database named Database1.<br/>The database contains a table named Customers.<br/>The Customer table has a column named email_address.<br/>You want to implement dynamic data masking to hide the data in the email_address column.<br/><br/>To achieve the requirement, you run the the Set-AzSqlDatabaseDataMaskingRule<br/>-DatabaseName " Database1" -SchemaName "dbo"<br/>-TableName "Customers" -ColumnName "email_address"<br/>-MaskingFunction "email" Powershell cmdlet<br/><br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>The Set-AzSqlDatabaseDataMaskingRule cmdlet sets a data masking rule for an Azure SQL database. To use the cmdlet, provide the ResourceGroupName, ServerName, DatabaseName, and RuleId parameters to identify the rule. You can provide any of the parameters of SchemaName, TableName, and ColumnName to retarget the rule. Specify the MaskingFunction parameter to modify how the data is masked.<br/>https://docs.microsoft.com/en-us/powershell/module/az.sql/set-azsqldatabasedatamaskingrule?view=azps-6.4.0&viewFallbackFrom=azps-6.2.1</details>|
|22|You are developing a web application.<br/>You want to use Azure Key Vault to ensure that sign-ins to the web application are secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>What should you do on the Web Application?<br/><br/>a. Enable AAD Connect<br/>b. Enable Managed Service Identity (MSI)<br/>c. Create an AAD service principal|<details><summary>Answer</summary>b. Enable Managed Service Identity (MSI)<br/><br/>A managed identity from Azure Active Directory (Azure AD) allows your app to easily access other Azure AD-protected resources such as Azure Key Vault. The identity is managed by the Azure platform and does not require you to provision or rotate any secrets.<br/>https://docs.microsoft.com/en-us/azure/app-service/overview-managed-identity?tabs=dotnet</details>|
|23|You are developing a web app that uses Azure Active Directory (Azure AD) for authentication.<br/>You want to configure the web app to use multifactor authentication.<br/>What should you do?<br/><br/>a. Enable mobile app authentication<br/>b. In AAD conditional access, enable the baseline policy<br/>c. In AAD, create a conditional access policy<br/>d. Install the Azure Multi-Factor Authentication Server|<details><summary>Answer</summary>c. In AAD, create a conditional access policy<br/><br/>Conditional Access policies at their simplest are if-then statements, if a user wants to access a resource, then they must complete an action. Example: A payroll manager wants to access the payroll application and is required to perform multi-factor authentication to access it.<br/>https://docs.microsoft.com/en-us/azure/active-directory/conditional-access/overview</details>|
|24|You have an Azure Active Directory (Azure AD) environment.<br/>Sometimes, users connect to Azure AD via the Internet.<br/>You need to ensure that users who connect to Azure AD via the internet using an unidentified IP address, are automatically instructed to change their passwords.<br/><br/>To achieve the requirement, you configure the use of Azure Key Vault.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>Instead use Identity Protection. Identity Protection is a tool that allows organizations to accomplish three key tasks:<br/>1. Automate the detection and remediation of identity-based risks<br/>2. Investigate risks using data in the portal.<br/>3. Export risk detection data to your SIEM<br/>https://docs.microsoft.com/en-us/azure/active-directory/identity-protection/howto-sign-in-risk-policy</details>|
|25|You have an Azure Active Directory (Azure AD) environment.<br/>Sometimes, users connect to Azure AD via the Internet.<br/>You need to ensure that users who connect to Azure AD via the internet using an unidentified IP address, are automatically instructed to change their passwords.<br/><br/>To achieve the requirement, you configure the use of Azure AD Identity Protection.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>Identity Protection is a tool that allows organizations to accomplish three key tasks:<br/>1. Automate the detection and remediation of identity-based risks.<br/>2. Investigate risks using data in the portal.<br/>3. Export risk detection data to your SIEM.<br/>https://docs.microsoft.com/en-us/azure/active-directory/identity-protection/howto-sign-in-risk-policy</details>|
|26|You have an Azure Active Directory (Azure AD) environment.<br/>Sometimes, users connect to Azure AD via the Internet.<br/>You need to ensure that users who connect to Azure AD via the internet using an unidentified IP address, are automatically instructed to change their passwords.<br/><br/>To achieve the requirement, you configure the use of Azure AD Privileged Identity Management.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>Instead use Identity Protection. Identity Protection is a tool that allows organizations to accomplish three key tasks:<br/>1. Automate the detection and remediation of identity-based risks.<br/>2. Investigate risks using data in the portal.<br/>3. Export risk detection data to your SIEM.<br/>https://docs.microsoft.com/en-us/azure/active-directory/identity-protection/howto-sign-in-risk-policy</details>|
|27|You have an Azure SQL database that uses Azure AD authentication.<br/>You need to make sure that database developers can connect to the SQL database via Microsoft SQL Server Management Studio (SSMS).<br/>You also need to make sure the developers use their on-premises Active Directory account for authentication.<br/>The authentication prompts should be kept to a minimum.<br/><br/>Which of the following should you implement?<br/><br/>a. AAD token<br/>b. Azure Multi-Factor authentication<br/>c. Active Directory integrated authentication<br/>d. OATH software tokens|<details><summary>Answer</summary>c. Active Directory integrated authentication<br/><br/>Active Directory integrated authentication - Use this method if you are logged into Windows using your Azure Active Directory credentials from a federated domain, or a managed domain that is configured for seamless single sign-on for pass-through and password hash authentication.<br/>https://docs.microsoft.com/en-us/azure/azure-sql/database/authentication-aad-configure?tabs=azure-powershell</details>|
|28|You are developing an application that stores keys, secrets, and certificates in Azure Key Vault.<br/><br/>The application makes use of the Azure Key Vault APIs.<br/><br/>You want to configure the application to allow recovery of an accidental deletion of the key vault or key vault objects for 90 days after deletion.<br/><br/>What should you do?<br/><br/>a. Run the Add-AzKeyVaultKey cmdlet<br/>b. Run the az keyvault update --enable-soft-delete true --enable-purge-protection true CLI<br/>c. Implement virtual network service endpoint for Azure Key Vault<br/>d. Run the az keyvault update --enable-soft-delete false CLI|<details><summary>Answer</summary>b. Run the az keyvault update --enable-soft-delete true --enable-purge-protection true CLI<br/><br/>Key Vault's soft-delete feature allows recovery of the deleted vaults and deleted key vault objects (for example, keys, secrets, certificates), known as soft-delete.<br/>When soft-delete is enabled, resources marked as deleted resources are retained for a specified period (90 days by default).<br/>When the --enable-purge-protection flag is enabled on the vault itself. In this case, Key Vault will wait for 90 days from when the original secret object was marked for deletion to permanently delete the object.<br/>https://docs.microsoft.com/en-us/azure/key-vault/general/soft-delete-overview</details>|
|29|You have developed a Web App in Azure.<br/>You need to create an alert whenever the Web App uses more than 85 percent of the available CPU cores over a 5 minute period.<br/><br/>Which command should you use?<br/><br/>a. az monitor metrics alert create -n alert1 -g myresourcegroup --scopes resourceId --condition "avg Percentage CPU >85" --window-size 5m<br/>b. az monitor metrics alert create -n alert1 -g myresourcegroup --scopes resourceId --condition "CPU Usage > 85" --window-size 5m<br/>c. az monitor metrics alert create -n alert1 -g myresourcegroup --scopes resourceId --condition "avg Percentage CPU > 85" -evaluation-frequency 5m<br/>d. az monitor metrics alert create -n alert1 -g myresourcegroup --scopes resourceId --condition "CPU Usage > 85" -evaluation-frequency 5m|<details><summary>Answer</summary>a. az monitor metrics alert create -n alert1 -g myresourcegroup --scopes resourceId --condition "avg Percentage CPU > 85" --window-size 5m<br/><br/>Avg percentage CPU provides the usage of CPU and window size is the time over which to aggregate metrics in "##h##m##s" format.<br/>https://docs.microsoft.com/en-us/cli/azure/monitor/metrics/alert?view=azure-cli-latest#az_monitor_metrics_alert_create</details>|
|30|You are configuring a web app that delivers streaming video to users.<br/>You need to ensure that the users streaming experience is constant.<br/>You also want to configure the application to store data in a geographic location that is nearest to the user.<br/><br/>To achieve the requirement, you include the use of Azure Redis Cache in your design.<br/><br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. no<br/><br/>Azure Redis Cache is used for server side caching. Instead, you can use Content Delivery Network (CDN). A content delivery network (CDN) is a distributed network of servers that can efficiently deliver web content to users. CDNs store cached content on edge servers that are close to end users to minimize latency.<br/>https://docs.microsoft.com/en-us/azure/architecture/best-practices/cdn</details>|
|31|You are configuring a web app that delivers streaming video to users.<br/>You need to ensure that the users streaming experience is constant.<br/>You also want to configure the application to store data in a geographic location that is nearest to the user.<br/><br/>To achieve the requirement, you include the use of an Azure Content Delivery Network (CDN) in your design.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>a. yes<br/><br/>You can use Content Delivery Network (CDN). A content delivery network (CDN) is a distributed network of servers that can efficiently deliver web content to users. CDNs store cached content on edge servers that are close to end users to minimize latency.<br/>https://docs.microsoft.com/en-us/azure/architecture/best-practices/cdn</details>|
|32|You are configuring a web app that delivers streaming video to users.<br/>You need to ensure that the users streaming experience is constant.<br/>You also want to configure the application to store data in a geographic location that is nearest to the user.<br/><br/>To achieve the requirement, you include the use of a Storage Area Network (SAN) in your design.<br/>Did you achieve the requirement?<br/><br/>a. yes<br/>b. no|<details><summary>Answer</summary>b. No<br/><br/>SAN storage is to store files on a server. Instead, you can use Content Delivery Network (CDN). A content delivery network (CDN) is a distributed network of servers that can efficiently deliver web content to users. CDNs store cached content on edge servers that are close to end users to minimize latency.<br/>https://docs.microsoft.com/en-us/azure/architecture/best-practices/cdn</details>|
|33|You have two Hyper-V hosts named Host1 and Host2.<br/>Host1 has an Azure virtual machine named VM1 that was deployed by using a custom Azure Resource Manager template.<br/>You need to move VM1 to Host2<br/>What should you do?<br/><br/>a. From the Update management blade, click Enable<br/>b. From the Overview blade, move VM1 to a different subscription<br/>c. From the Redeploy blade, click Redeploy<br/>d. From the Profile blade, modify the usage location|<details><summary>Answer</summary>c. From the Redeploy blade, click Redeploy<br/><br/>When you redeploy a VM, Azure will shut down the VM, move the VM to a new node within the Azure infrastructure, and then power it back on, retaining all your configuration options and associated resources.<br/>https://docs.microsoft.com/en-us/troubleshoot/azure/virtual-machines/redeploy-to-new-node-windows</details>|
|34|You have downloaded an Azure Resource Manager template to deploy numerous virtual machines.<br/>The template is based on a current virtual machine, but must be adapted to reference an administrative password.<br/>You need to make sure that the password is not stored in plain text.<br/>You are preparing to create the necessary components to achieve your goal.<br/><br/>What should you create to store the password?<br/><br/>a. an Azure Key Vault & an access policy<br/>b. an Azure Storage account & an access policy<br/>c. a Recovery Services vault & a backup policy<br/>d. AAD Identity Protection & Azure policy|<details><summary>Answer</summary>a. an Azure Key Vault & an access policy<br/><br/>an Azure Key Vault and an access policy - When you create a virtual machine (VM). You need to provide the VM administrator username and password. Instead of providing the password, you can pre-store the password in an Azure key vault and then customize the template to retrieve the password from the key vault during the deployment.<br/>https://docs.microsoft.com/en-us/azure/azure-resource-manager/templates/template-tutorial-use-key-vault</details>|
|35|You have a new Azure subscription.<br/>You are developing an internal website for employees to view sensitive data.<br/>The website uses Azure Active Directory (Azure AD) for authentication.<br/><br/>You need to implement multifactor authentication for the website.<br/>Which two actions should you perform?<br/><br/>a. Configure the website to use AAD B2C<br/>b. in AAD, create a new conditional access policy<br/>c. Upgrade to AAD Premium<br/>d. In AAD, enable application proxy<br/>e. In AAD conditional access, enable the baseline policy|<details><summary>Answer</summary>b. In AAD, create a new conditional access policy<br/>c. Upgrade to AAD Premium<br/><br/>Multi-factor authentication (MFA) is a process in which a user is prompted for additional forms of identification during a sign-in event. For example, the prompt could be to enter a code on their cellphone or to provide a fingerprint scan. When you require a second form of identification, security is increased because this additional factor isn't easy for an attacker to obtain or duplicate.<br/>Azure AD Multi-Factor Authentication and Conditional Access policies give you the flexibility to require MFA from users for specific sign-in events.<br/>Prerequisite to configure conditional access policy is at least an Azure AD Premium P1 license.<br/>https://docs.microsoft.com/en-us/azure/active-directory/authentication/tutorial-enable-azure-mfa</details>|
|36|You plan to deploy a new application to a Linux virtual machine (VM) that is hosted in Azure.<br/>The entire VM must be secured at rest by using industry-standard encryption technology to address organizational security and compliance requirements.<br/>You need to configure Azure Disk Encryption for the VM.<br/>How should you complete the Azure CLI commands?<br/>`az provider register -n Microsoft.KeyVault`<br/>`resourcegroup = "myResourcegroup`<br/>`az group create --name $resourcegroup --location eastus`<br/>`keyvault_name = myvaultname$RANDOM`<br/>` `<br/>`az <CodeBlock1 create --name $keyvault_name --resourcegroup $resourcegroup --location east \ `<br/>`--enabled-for-disk-encryption true`<br/>`az <CodeBlock2> create --vault-name $keyvault_name --name name1 --protection software`<br/>`az <CodeBlock3> create --resource-group $resourcegroup --name "myVM" \ `<br/>`--image "Canonical:UbuntuServer:16.04-LTS:latest"\`<br/>`--admin-username azureuser\`<br/>`--size "Standard_D2S_v3"\`<br/>`--generate--sh-keys`<br/>`az <CodeBlock4 --resource-group $resourcegroup --name "myVM"\`<br/>`--disk-encryption-key Name1\`<br/>`--volume-type <CodeBlock5>`<br/><br/>a. CodeBlock1:keyvault<br/>b. CodeBlock1:vm<br/>c. CodeBlock2:keyvault<br/>d. CodeBlock2:keyvault key<br/>e. CodeBlock3:vm<br/>f. CodeBlock3:vm encryption<br/>g. CodeBlock4:vm<br/>h. CodeBlock4:vm encryption<br/>i. CodeBlock5:all<br/>j. CodeBlock5:data<br/>k. CodeBlock5:os|<details><summary>Answer</summary>a. CodeBlock1:keyvault<br/>d. CodeBlock2:keyvault key<br/>e. CodeBlock3:vm<br/>h. Codeblock4:vm encryption<br/>i. CodeBlock5:all<br/><br/>https://docs.microsoft.com/en-us/azure/virtual-machines/linux/disk-encryption-cli-quickstart</details>|
|37|You are developing an application. You have an Azure user account that has access to two subscriptions.<br/>You need to retrieve a storage account key and store it as a secret in Azure Key Vault.<br/><br/>In which order should you arrange the PowerShell commands to develop the solution?<br/>`1 $secretvalue = ConvertTo-SecureString $storAcctKey -AsPlainText -Force`<br/>`Set-AzKeyVaultSecret -VaultName $vaultName -Name $secretName -SecretValue $secretvalue`<br/><br/>`2 Get-AzStorageAccountKey -ResourceGroupName $resGroup -Name $storAcct`<br/><br/>`3 Set-AzContext -SubscriitonId $subscriptionID`<br/><br/>`4 Get-AzKeyVaultSecret -VaultName $vaultName`<br/><br/>`5 Get-AzSubscription`<br/><br/>a. 5,4,2,3,1<br/>b. 3,4,2,5,1<br/>c. 5,3,2,3,1<br/>d. 5,3,2,4,1<br/>e. 5,3,2,1,4|<details><summary>Answer</summary>e. 5,3,2,1,4<br/><br/>Step 1: Get-AzSubscription -<br/>When you have multiple subscriptions, you might have to specify the one that was used to create your key vault.<br/>Step 2: Set-AzContext -SubscriptionId<br/>To specify the subscription that's associated with the key vault you'll be logging<br/>Step 3: Get-AzStorageAccountKey -<br/>You must get that storage account key<br/>Step 4 : $secretvalue = ConvertTo-SecureString <storageAccountKey> -AsPlainText -Force<br/>Set-AzKeyVaultSecret -VaultName <vaultName> -Name <secretName> -SecretValue $secretvalue<br/>After retrieving your storage account key, you must convert that key to a secure string, and then create a secret with that value in your key vault.<br/>Step 5: Get-AzKeyVaultSecret -<br/>Get the URI for the secret you created.<br/>https://docs.microsoft.com/bs-latn-ba/Azure/key-vault/key-vault-key-rotation-log-monitoring</details>|
|38|You are developing an Azure App Service REST API.<br/>The API must be called by an Azure App Service web app.<br/>The API must retrieve and update user profile information stored in Azure Active Directory (Azure AD).<br/>You need to configure the API to make the updates.<br/>Which two tools should you use?<br/><br/>a. Microsoft graph API<br/>b. MSAL<br/>c. Azure API Management<br/>d. Microsoft Defender<br/>e. Microsoft Azure Key Vault SDK|<details><summary>Answer</summary>a. Microsoft Graph API<br/>b. MSAL<br/><br/>The Microsoft Authentication Library (MSAL) enables developers to acquire tokens from the Microsoft identity platform in order to authenticate users and access secured web APIs.<br/>You can update user profile information using Graph API.</details>|
|39|You develop a web app that uses tier D1 app service plan by using the Web Apps feature of Microsoft Azure App Service.<br/>Spikes in traffic have caused increases in page load times.<br/>You need to ensure that the web app automatically scales when CPU load is about 85 percent and minimize costs.<br/>Which four actions should you perform in sequence?<br/><br/>1. Configure the web app to the Premium App Service tier<br/>2. Confiugre the web app to the Standard App Service tier<br/>3. Enable autoscaling on the web app<br/>4. Add a Scale rule<br/>5. Switch to an Azure App Services consumption plan<br/>6. Configure a Scale condition<br/><br/>a. 1,3,4,6<br/>b. 5,3,4,6<br/>c. 2,3,4,6<br/>d. 1,3,6,4<br/>e. 5,3,6,4<br/>f. 2,3,6,4|<details><summary>Answer</summary>f. 2,3,6,4<br/><br/>D1 app service tier does not support scaling. So, you must change it to Standard App Service tier. Standard tier also adheres to minimum costs.<br/>The steps are as below.<br/>Enable web app to autoscale (custom autoscale)<br/>When you implement autoscaling, you can edit the default scale condition, and you can add your own custom scale conditions.<br/>A metric-based scale condition contains one or more scale rules. Initially, a scale condition contains only a default rule. You use the Add a rule link to add your own custom rules.<br/>https://docs.microsoft.com/en-us/learn/modules/app-service-autoscale-rules/6-autoscale-a-web-app</details>|
|40|You are developing applications for a company.<br/>You plan to host the applications on Azure App Services.<br/>The company has the following requirements:<br/>Every five minutes verify that the websites are responsive.<br/>Verify that the websites respond within a specified time threshold. Dependent requests such as images and JavaScript files must load properly.<br/>Generate alerts if a website is experiencing issues.<br/>If a website fails to load, the system must attempt to reload the site three more times.<br/><br/>You need to implement this process with the least amount of effort.<br/>What should you do?<br/><br/>a. Create a selenium web test & configure it to run from your workstation as a scheduled task<br/>b. Set up a URL pin test to query the ome page<br/>c. Create an Azure function to query the home page<br/>d. Create a multi-step web test to query the home page<br/>e. Create a Custom Track Availability Test to query the home page|<details><summary>Answer</summary>b. set up a URL ping test to query the home page<br/><br/>URL ping test validate whether an endpoint is responding. It also measures the performance associated with that response. You can also set custom success criteria, coupled with more advanced features like parsing dependent requests and allowing for retries.<br/>https://docs.microsoft.com/en-us/azure/azure-monitor/app/monitor-web-app-availability</details>|
|41|You are a developer for a Software as a Service (SaaS) company.<br/>You develop solutions that provide the ability to send notifications by using Azure Notification Hubs.<br/>You need to create sample code that customers can use as a reference for how to send raw notifications to Windows Push Notification Services (WNS) devices.<br/>The sample code must not use external packages.<br/>How should you complete the below code segment?<br/>`var endpoint = "...";`<br/>`var payload = "...";`<br/>`var request = new HttpRequestMessage(HttpMethod.Post, endpoint);`<br/>`request.Headers.Add("X-WNS-Type", "wns/raw");`<br/>`request.Headers.Add("ServiceBusNotification-Format", "<CodeBlock1>";`<br/>`request.Content = new StringContent(payload, Encoding.UTF8, "<CodeBlock2>");`<br/>`var client = new HttpClient();`<br/>`await client.SendAsync(request);`<br/><br/>a. CodeBlock1:raw<br/>b. CodeBlock1:windows<br/>c. CodeBlock1:windowsphone<br/>d. CodeBlock2:application/xml<br/>e. CodeBlock2:application/json<br/>f. Codeblock2:application/octet-stream|<details><summary>Answer</summary>b. CodeBlock:windows<br/>f. CodeBlock2:application/octet-stream<br/><br/><table><tbody><tr><td>**Request header**</td><td>**Description**</td></tr><tr><td>Authorization</td><td>Token generated as specified in SAS with Service Bus, or Service Bus authentication & authorization with Microsoft AAD Access Control (also known as Access Control Service or ACS)</td></tr><tr><td>Content-Type</td><td>Set to application/json;charset-utf-8 or application/xml. If the notification type X-WNS-Type is wns/raw, set to application/octet-stream</td></tr><tr><td>ServiceBusNotification-Tags</td><td>{single tag identifier} (optional)</td></tr><tr><td>ServiceBusNotification-Format</td><td>Set to windows</td></tr><tr><td>X-WNS-</td><td>All headers starting with X-WNS will be forwarded to WNS</td></tr></tbody></table><br/>https://docs.microsoft.com/en-us/rest/api/notificationhubs/send-wns-native-notification</details>|
|42|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to deploy a new version of the LabelMaker application to ACR.<br/>Which three actions should you perform in sequence?<br/>1. Log in to the registry & push image<br/>2. Create an alias of the image with a new build number<br/>3. Create an alis of the image with the fully qualified path to the registry<br/>4. Download the image to your local computer<br/>5. Build a new application image by using dockerfile<br/><br/>a. 4,2,1<br/>b. 4,3,1<br/>c. 5,3,1<br/>d. 5,2,1|<details><summary>Answer</summary>c. 5,3,1<br/><br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/><br/>The steps are as follows:<br/>1. You need to build a continer image.<br/>2. You need to tag the image in order to be able to push it to registry<br/>3. You need to login to ACR and push image from CLI.<br/>https://docs.microsoft.com/en-us/azure/container-registry/container-registry-tutorial-quick-task</details>|
|43|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to access data from the user claim object in the e-commerce web app.<br/>What should you do first?<br/><br/>a. Write custom code to make a Microsoft Graph API call from the e-commerce web app<br/>b. Assign the Contributor RBAC role to the e-commerce web app by using the Resource Manager create role assignment API<br/>c. Update the e-commerce web app to read the HTTP request header values<br/>d. Using the Azure CLI, enable CORS from the e-commerce checkout API to the e-commerce web app|<details><summary>Answer</summary>c. Update the e-commerce web app to read the HTTP request header values<br/><br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>You can extract claims associated to an authenticated user from HTTP request.<br/>The following example extracts the claims associated to the authenticated user performing an HTTP request and writes them in the HTTP response. The current user is read from the HttpContext as a ClaimsPrincipal and the claims are read from it.<br/>`ClaimsPrincipal principal = HttpContext.Current.User as ClaimsPrincipal;`<br/>`if (null != principal)`<br/>`{`<br/>`foreach (Claim claim in principal.Claims)`<br/>`{`<br/>`Response.Write("CLAIM TYPE: " + claim.Type + "; CLAIM VALUE: " + claim.Value + " </br>");`<br/>`}`<br/>`}`<br/>https://docs.microsoft.com/en-us/dotnet/api/system.security.claims.claim?view=net-5.0</details>|
|44|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to configure Azure Cosmos DB.<br/>Which settings should you use?<br/><br/>a. Consistency Level:Strong<br/>b. Consistency Level:Bounded-staleness<br/>c. Consistency Level:Session<br/>d. Consistency Level:Eventual<br/>e. API:SQL<br/>f. API:MongoDB<br/>g. API:Graph<br/>h. API:Table|<details><summary>Answer</summary>a. Consistency Level:Strong<br/>e. API:SQL<br/><br/>Scenario: Order data is stored as nonrelational JSON and must be queried using SQL.Changes to the Order data must reflect immediately across all partitions. All reads to the Order data must fetch the most recent writes.<br/>Strong consistency offers a linearizability guarantee. Linearizability refers to serving requests concurrently. The reads are guaranteed to return the most recent committed version of an item. A client never sees an uncommitted or partial write. Users are always guaranteed to read the latest committed write.<br/>The requirement is to query using SQL.<br/>https://docs.microsoft.com/en-us/azure/cosmos-db/consistency-levels</details>|
|45|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to retrieve all order line items from Order.json and sort the data alphabetically by the city.<br/>How should you complete the code?<br/>`SELECT li.id AS lineitemid, li.price`<br/>`FROM <CodeBlock1>`<br/>`JOIN <CodeBlock2> IN <CodeBlock3>`<br/>`ORDER by <CodeBlock4> ASC`<br/><br/>a. CodeBlock1:Orders o<br/>b. CodeBlock1:LineItem li<br/>c. CodeBlock2:li<br/>d. CodeBlock2:o<br/>e. CodeBlock3:o.line_items<br/>f. CodeBlock3:li.line_items<br/>g. CodeBlock3:o.address<br/>h. CodeBlock4:o.address.city<br/>i. CodeBlock4:li.address.city<br/>j. CodeBlock4:o.city<br/>k. CodeBlock4:li.city|<details><summary>Answer</summary>a. CodeBlock1:Orders o<br/>c. CodeBlock2:li<br/>e. CodeBlock3:o.line_itemsh. CodeBlock4:oaddress.city<br/><br/>Scenario: Order data is stored as nonrelational JSON and must be queried using SQL.<br/>You need to retrieve all order line items, so join orders with lineitmes and sort by city. The city field is in Order</details>|
|46|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to troubleshoot the order workflow.<br/>Which two actions should you perform?<br/><br/>a. Review the API connections<br/>b. Review the activity log<br/>c. Review the run history<br/>d. Review the trigger history|<details><summary>Answer</summary>c. Review the run history<br/>d. Review the trigger history<br/><br/>Scenario: The order workflow fails to run upon initial deployment to Azure.<br/>Each time that the trigger fires, Azure Logic Apps creates a workflow instance and runs that instance. If a run fails, you can review what happened during that run. You can review the status, inputs, and outputs for each step in the workflow.<br/>Each workflow run starts with a trigger, which either fires on a schedule or waits for an incoming request or event. The trigger history lists all the trigger attempts that your workflow made and information about the inputs and outputs for each trigger attempt. If a trigger fails, you can review the information to find the root cause.<br/>https://docs.microsoft.com/en-us/azure/logic-apps/logic-apps-diagnosing-failures?tabs=consumption</details>|
|47|**LabelMaker app -**<br/>Cola Winery produces, bottles, and distributes a variety of wines globally.<br/>You are a developer implementing highly scalable and resilient applications to support online order processing by using Azure solutions.<br/>Cola Winery has a LabelMaker application that prints labels for wine bottles.<br/>The application sends data to several printers.<br/>The application consists of five modules that run independently on virtual machines (VMs).<br/>Cola Winery plans to move the application to Azure and continue to support label creation.<br/>External partners send data to the LabelMaker application to include artwork and text for custom label designs.<br/><br/>**Requirements -**<br/>**Data -**<br/>You identify the following requirements for data management and manipulation:<br/>Order data is stored as nonrelational JSON and must be queried using SQL.<br/>Changes to the Order data must reflect immediately across all partitions.<br/>All reads to the Order data must fetch the most recent writes.<br/><br/>**Requirements -**<br/>**Secuirty -**<br/>You have the following security requirements:<br/>Users of Cola Winery applications must be able to provide access to documents, resources, and applications to external partners.<br/>External partners must use their own credentials and authenticate with their organization's identity management solution.<br/>External partner logins must be audited monthly for application use by a user account administrator to maintain company compliance.<br/>Storage of e-commerce application settings must be maintained in Azure Key Vault.<br/>E-commerce application sign-ins must be secured by using Azure App Service authentication and Azure Active Directory (AAD).<br/>Conditional access policies must be applied at the application level to protect company content.<br/>The LabelMaker application must be secured by using an AAD account that has full access to all namespaces of the Azure Kubernetes Service (AKS) cluster.<br/><br/>**Requirements -**<br/>**LabelMaker app -**<br/>Azure Monitor Container Health must be used to monitor the performance of workloads that are deployed to Kubernetes environments and hosted on Azure Kubernetes Service (AKS).<br/>You must use Azure Container Registry to publish images that support the AKS deployment.<br/>**Architecture -**<br/>[<img src="https://i.imgur.com/am36gxQ.png">](https://i.imgur.com/am36gxQ.png)<br/>**Issues -**<br/>Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds. The label printer must only receive up to 5 attempts within one minute.<br/>The order workflow fails to run upon initial deployment to Azure.<br/><br/>**Order.json -**<br/>Relevant portions of the app files are shown below.<br/>This JSON file contains a representation of the data for an order that includes a single item.<br/>**Order.json -**<br/>`{`<br/>`    "id": 1,`<br/>`    "customers" : [`<br/>`        {`<br/>`            "familyName" : "Doe",`<br/>`            "givenName" : "John",`<br/>`            "customerid" : 5`<br/>`        }`<br/>`    ],`<br/>`    "line_items" : [`<br/>`        {`<br/>`            "fulfillable_quantity" : 1,`<br/>`            "id" : 6,`<br/>`            "price" : "199.99",`<br/>`            "product_id" : 7513594,`<br/>`            "quantity" : 1,`<br/>`            "requires_shpping" : true,`<br/>`            "sku" : "SFC-342-N",`<br/>`            "title" : "Surface Go",`<br/>`            "vendor" : "Microsoft",`<br/>`            "name" : "Surface Go - 8GB",`<br/>`            "taxable" : true,`<br/>`            "tax_lines" : [`<br/>`                {`<br/>`                    "title" : "State Tax",`<br/>`                    "price" : "3.98",`<br/>`                    "rate" : 0.06`<br/>`                }`<br/>`            ],`<br/>`            "total_discount" : "5.00",`<br/>`            "discount_allocations" : [`<br/>`                {`<br/>`                    "amount" : "5.00",`<br/>`                    "discount_application_index" : 2`<br/>`                }`<br/>`            ]`<br/>`        }`<br/>`    ]`<br/>`}`<br/>**Question**<br/>You need to update the order workflow to address the issue when calling the Printer API App.<br/>How should you complete the code?<br/>`"print_label" : {`<br/>`	"type" : "Http",`<br/>`	"inputs" : {`<br/>`		"method" : "POST",`<br/>`		"uri" : "https://ww.cohowinery.com/printer/printlabel",`<br/>`		"retryPolicy" : {`<br/>`			"type" : "<CodeBlock1>",`<br/>`			"interval" : "<CodeBlock2>",`<br/>`			"count" : "<CodeBlock3>"`<br/>`		}`<br/>`	}`<br/>`}`<br/>a. CodeBlock1:default<br/>b. CodeBlock1:none<br/>c. CodeBlock1:fixed<br/>d. Codeblock1:exponential<br/>e. CodeBlock2:PT10S<br/>f. CodeBlock2:PT30S<br/>g. CodeBlock2:PT60S<br/>h. CodeBlock2:PT1D<br/>i. CodeBlock3:5<br/>j. CodeBlock3:10<br/>k. CodeBlock3:60|<details><summary>Answer</summary>c. CodeBlock1:fixed<br/>f. CodeBlock2:PT30S<br/>i. CodeBlock3:5<br/><br/>Scenario: Calls to the Printer API App fail periodically due to printer communication timeouts.<br/>Printer communication timeouts occur after 10 seconds.<br/>The label printer must only receive up to 5 attempts within one minute.<br/>To specify that the action or trigger waits the specified interval before sending the next request, set the <retry-policy-type> to fixed.<br/>The printer app must receive upto 5 attempts only. It means fixed number re-tries per minute. So maximum number of retries are 5. Count provides the number of retry attempts.<br/>The timeout occurs after 10 seconds. So, retry interval value can be anything more than 10 Seconds. In this case, if we set to 30 seconds, then number of retries will be less than 5 per minute.<br/>https://docs.microsoft.com/en-us/azure/logic-apps/logic-apps-exception-handling?tabs=consumption</details>|

---

## Results
|n|Date|Note|Revison|
|-|----|----|-------|
|1|14-08-2023 PM|22/47 = 46%|<details><summary>Revision</summary>True</details>|
|2|16-08-2023 PM|44/47 = 93%|<details><summary>Revision</summary>Q08 You are developing an online ordering application that uses Azure Cosmos DB to store orders.<br/>The latest SDK is used to manage the orders in the database.You create a new Azure Cosmos DB instance.You include a valid endpoint and valid authorization key to an appSettings.json file in the code project.<br/>You are evaluating the below application code<br/>`using Sysstem;`<br/>`using Threading.Task;`<br/>`using Microsoft.Azure.Cosmos;`<br/>`using Microsoft.Extensions.Configuration;`<br/>`using Newonsoft.Json;`<br/>` `<br/>`namespace SalesOrders`<br/>`{`<br/>`    public class SalesOrders`<br/>`    {`<br/>`        ...`<br/>`    }`<br/>` `<br/>`    internal class ManageSalesOrders`<br/>`    {`<br/>`        private static asyn Task GenerateSalesOrders()`<br/>`				{`<br/>`					IConfigurationRoot configuration = new ConfigurationBuilder().AddJsonFile("appSettings.json").Build();`<br/>`					string endpoint = configuration["EndpointUrl]";`<br/>`					string authKey = configuration["AuthorizationKey"];`<br/>`					using CosmosClient client = new CosmosClient(endpoint, authKey);`<br/>`					Database database = null;`<br/>`					using (await client.GetDatabase("SalesOrders".DeleteStreamAsync())) {}`<br/>`					database = await client.CreateDatabaseIfNotExistAsync("SalesOrders");`<br/>`Container container1 = await database.CreateContainerAsync(id: "Container1",partitionKeyPath: "/AccountNumber");`<br/>`					Container container2 = await database.CreateContainerAsync(id: "Container2",partitionKeyPath: "/AccountNumber");`<br/>`					SalesOrders salesOrder1 = new SalesOrders() { AccountNumber = "123456" };`<br/>`					await container1.CreateItemAsync(salesOrder1, new partitionKey(salesOrder1.AccountNumber));`<br/>`					SalesOrders salesOrder2 = new SalesOrders() { AccountNumber = "654321" };`<br/>`					await container1.CreateItemAsync(salesOrder2, new partitionKey(salesOrder2.AccountNumber));`<br/>`					SalesOrders salesOrder3 = new SalesOrders() { AccountNumber = "109876" };`<br/>`					await container2.CreateItemAsync(salesOrder3, new partitionKey(salesOrder3.AccountNumber));`<br/>`					_ = await database.CreateUserAsync("User1");`<br/>`					User user1 = database.GetUser("User1");`<br/>`					_ = await user1.ReadAsync();`<br/>`				}`<br/>`    }`<br/>`}`<br/>Select Yes if the below statement is true. Otherwise, select No.<br/>1. A database named SalesOrders is created and it will contain two containers.<br/>2. Container1 will contain two items.<br/>3. Container2 will contain one item.<br/><br/>a. yes, no, yes<br/>b. yes, yes, yes<br/>c. no, yes, no<br/>d. yes, yes, no<br/>e. yes, no, no<br/><br/>Q11 You have an Azure Web app that uses Cosmos DB as a data store. You create a CosmosDB container by running the following PowerShell script:<br/>`$resourceGroupName = "testResourceGroup"`<br/>`$accountName = "testCosmosAccount"`<br/>`$databaseName = "testDatabase"`<br/>`$containerName = "testContainer"`<br/>`$partitionKeyPath = "/EmployeeId"`<br/>`$autoscaleMaxThroughput = 5000`<br/><br/>`New-AzCosmosDBSqlContainer -`<br/>`-ResourceGroupName $resourceGroupName`<br/>`-AccountName $accountName`<br/>`-DatabaseName $databaseName`<br/>`-Name $containerName`<br/>`-PartitionKeyKind Hash`<br/>`-PartitionKeyPath $partitionKeyPath`<br/>`-AutoscaleMaxThroughput $autoscaleMaxThroughput`<br/><br/>You create the following queries that target the container:<br/>`SELECT * FROM c WHERE c.EmployeeId > '12345'`<br/>`SELECT * FROM c WHERE c.UserID = '12345'`<br/><br/>Select Yes if the below statement is true. Otherwise, select No.<br/>1. The minimum throughput for the container is 400 RU/s<br/>2. The query SELECT * FROM c WHERE c.EmployeeId > '12345' is an in-partition query<br/>3. The querySELECT * FROM c WHERE c.UserID = '12345' is a cross partition query<br/><br/>a. yes, yes, yes<br/>b. no, yes, yes<br/>c. no, no, yes<br/>d. yes, no, yes<br/>e. yes, no, no<br/>f. no, yes, no<br/><br/>Q42 </details>|
|3|01-09-2023 AM|35/47 = 74%|<details><summary>Revision</summary>True</details>|
